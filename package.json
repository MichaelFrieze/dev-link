{
  "name": "dev-link",
  "version": "0.5.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean-install": "run-p clean-install:api clean-install:client",
    "clean-install:api": "cd api && npm ci",
    "clean-install:client": "cd client && npm ci",
    "start:api": "npm run server --prefix api",
    "start:client": "npm run start --prefix client"
  },
  "author": "Michael Frieze",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
